# FFmpeg / FFplay (retirado)

Desde **rc.6-fix7** magicTupper no descarga, ejecuta ni redistribuye `ffplay.exe`. La reproducción H.264 se realiza dentro del PC Client mediante Windows Media Foundation. Este archivo se conserva únicamente como nota histórica de las previews rc.6 anteriores.
